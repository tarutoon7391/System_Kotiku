<?php

// =====================================
// エラー表示切り替え用ファイル
// 学習中はエラーを表示したいことが多いですが、
// 本番では通常、詳細エラーは表示しません。
// =====================================

// $error_fog が 1 のときだけエラー表示をONにする
// ※ 変数名は既存に合わせています（おそらく error_flag の意図）
if($error_fog == 1)
{
    // display_errors = 1 で画面にエラーを表示
    ini_set('display_errors', 1);
    // error_reporting(E_ALL) ですべての種類のエラーを対象にする
    ini_set('error_reporting', E_ALL);
}

?>